# ====================================================================
# 《鏡之海》總宇宙開發專案 - 終極核心過濾清單 (.gitignore)
# ====================================================================

# --------------------------------------------------------------------
# 1. 獨立開發美術防線：強制排除或隔離區（不進 Git 也不進 LFS）
# --------------------------------------------------------------------
# 排除所有以波浪號結尾的原始美術原檔安全隔離區（2100x3600 PSD 與 3D 母帶的地方）
**/_RawAssets~/

# Blender 自動生成的備份存檔（1~5 全面防禦）
*.blend1
*.blend2
*.blend3
*.blend4
*.blend5

# Maya 的臨時快取、暫存與崩潰回復檔
*.swp
*.ma.tmp
*.mb.tmp
.mayaSwatches/
command-history

# --------------------------------------------------------------------
# 2. Unity 系統快取與打包防線（最外層與子目錄雙重防禦）
# --------------------------------------------------------------------
# Android / iOS 高階編譯 (IL2CPP) 本地偵錯快取
.utmp/

# Unity 核心三大巨大快取（開啟專案時自動生成，絕對不進 Git）
/[尝尝]颈产谤补谤测/
/[罢罢]别尘辫/
/[翱翱]产箩/

# 專案根目錄或 Unity 子目錄下的 Library 強力防禦
/[鲍鲍]苍颈迟测/[尝尝][尝尝]/
/[鲍鲍]苍颈迟测/[尝尝][尝尝][尝尝][尝尝][尝尝][尝尝]/
/[鲍鲍]苍颈迟测/[尝尝][尝尝][尝尝]/
/[鲍鲍]苍颈迟测/[尝尝][尝尝][尝尝][尝尝][尝尝][尝嘗][尝尝][尝尝][尝尝]/

# 本地編譯、打包輸出與導出資料夾
/[叠叠]耻颈濒诲/
/[叠叠]耻颈濒诲蝉/
/[鲍鲍]苍颈迟测/叠耻颈濒诲/
/[贰别虫辫辞谤迟]/

# 系統日誌與個人編輯器局部排版設定
/[尝尝]辞驳蝉/
/[尝尝]辞驳蝉/*.濒辞驳
UserSettings/
/[鲍鲍]蝉别谤[厂厂]别迟迟颈苍驳蝉/

# Unity Profiler 與效能快取資料夾
/[惭惭]别尘辞谤测[颁颁]补辫迟耻谤别蝉/
/[搁搁]别肠辞谤诲颈苍驳蝉/
/[鲍鲍]苍颈迟测/搁别肠辞谤诲颈苍驳蝉/

# Unity 編輯器崩潰報告產生的本地硬體資訊
sysinfo.txt

# --------------------------------------------------------------------
# 3. IDE 程式碼編輯器與跨平台編譯快取（Visual Studio & JetBrains）
# --------------------------------------------------------------------
.vs/
.idea/
.gradle/
.consulo/
ExportedObj/

# 自動產生的編譯過渡暫存檔
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# 排除已被阻擋的偵錯檔所對應的無意義 .meta 衍生檔
*.pidb.meta
*.pdb.meta
*.mdb.meta

# --------------------------------------------------------------------
# 4. 雲端與 Firebase 後端防線（安全憑證防禦區）
# --------------------------------------------------------------------
.firebase/
*firebase-adminsdk*.json
*secret*.json
*.local
crashlytics-build.properties

# --------------------------------------------------------------------
# 5. Mac / Windows 系統層級自動產生的垃圾縮圖與快取
# --------------------------------------------------------------------
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db