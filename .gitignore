# ====================================================================
# 《鏡之海-SEA OF MIRRORS》開發專案 - 核心過濾清單 (.gitignore)
# ====================================================================

# --------------------------------------------------------------------
# 1. 獨立開發美術防線：強制排除或隔離區（不進 Git 也不進 LFS）
# --------------------------------------------------------------------
# 排除所有以波浪號結尾的原始美術原檔安全隔離區（2100x3600 PSD 與 3D 母帶的地方）
**/_RawAssets~/

# Blender 自動生成的備份存檔（1~5 全面防禦）
*.blend1
*.blend2
*.blend3
*.blend4
*.blend5
*.blend@

Blender_Temp/

# Maya 的臨時快取、暫存與崩潰回復檔
*.mb~
*.ma~
*.swp
*.ma.tmp
*.mb.tmp
.mayaSwatches/
command-history

# Photoshop 超大圖層檔案
# 正常情況下 PSD/PSB 應放在 _RawAssets~/ 隔離區內，已被上方規則排除
# 若工作流程中可能存在隔離區以外的地方，取消下面註解作為最後防線
# *.psd
# *.psb

# --------------------------------------------------------------------
# 2. Unity 系統快取與打包防線（已優化：完美支援子目錄架構）
# --------------------------------------------------------------------
# Android / iOS 高階編譯 (IL2CPP) 本地偵錯快取
**/[Uu]tmp/

# Unity 核心三大巨大快取（移除前端斜線，確保不論在哪個層級一律全面封鎖）
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/

# 本地編譯、打包輸出與導出資料夾
/[Bb]uild/
/[Bb]uilds/
/[Ee]xport/

# 手機平台打包輸出檔（即使未放在 Build/ 資料夾內也排除）
*.apk
*.aab
*.ipa

# Unity Addressables 系統產生的 建置輸出資料夾
ServerData/

# 系統日誌與個人編輯器局部排版設定
/[Ll]ogs/
/[Ll]ogs/*.log
/[Uu]ser[Ss]ettings/

# Unity Profiler 與效能快取資料夾
/[Mm]emory[Cc]aptures/
/[Rr]ecordings/

# Unity 編輯器崩潰報告產生的本地硬體資訊
sysinfo.txt

# --------------------------------------------------------------------
# 3. IDE 程式碼編輯器與跨平台編譯快取（Visual Studio & JetBrains）
# --------------------------------------------------------------------
.vs/
.idea/
.gradle/
.consulo/
ExportedObj/

# 自動產生的編譯過渡暫存檔
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# VSCode
.vscode/

# 排除已被阻擋的偵錯檔所對應的無意義 .meta 衍生檔
*.pidb.meta
*.pdb.meta
*.mdb.meta

# --------------------------------------------------------------------
# 4. 雲端與 Firebase 後端防線（安全憑證防禦區）
# --------------------------------------------------------------------
.firebase/
*firebase-adminsdk*.json
*secret*.json
*.local
crashlytics-build.properties

# --------------------------------------------------------------------
# 5. Mac / Windows 系統層級自動產生的垃圾縮圖與快取
# --------------------------------------------------------------------
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# --------------------------------------------------------------------
# 6. Temporary
# --------------------------------------------------------------------
*.tmp
*.bak
*.old
